name: feature_flutter
description: Flutter components for managing feature-flags.
version: 1.1.0
repository: https://github.com/nogipx/feature

environment:
  sdk: ">=2.14.2 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.3
  feature_core: ^1.0.2

dependency_overrides:
  feature_core:
    path: ../feature_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: